spring:
  mvc:
    servlet:
      path: /api/v1
  application:
    name: reports
    cloud:
      config:
        enabled: true
        uri: http://localhost:8081
logging:
  level:
    org:
      hibernate:
        SQL: debug
springdoc:
  api-docs:
    path: /api-docs