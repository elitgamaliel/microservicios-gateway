server:
  port: 8081

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          default-label: master
          uri: https://github.com/elitgamaliel/microservicios-gateway
          search-paths: config-data
  